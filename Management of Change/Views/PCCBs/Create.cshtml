@model Management_of_Change.ViewModels.PccbVM

@{
    ViewData["Title"] = "Create";
}

<h1>PCCB Review</h1>

<h4 class="mt-5">Create</h4>
<hr />
<form asp-action="Create">
    <input type="hidden" asp-for="PCCB.ChangeRequestId" />
    <input type="hidden" asp-for="PCCB.CreatedUser" />
    <input type="hidden" asp-for="PCCB.CreatedDate" />
    <input type="hidden" asp-for="PCCB.Status" />
    @* <input type="hidden" asp-for="Invitees2" id="thiss" /> *@
    @Html.AntiForgeryToken()
    <div class="form-group my-3">
        <input type="submit" value="Create" class="btn btn-success" />
        @Html.ActionLink("Cancel", "Details", "ChangeRequests", new { id = Model.PCCB.ChangeRequestId, tab = "PccbReview" }, new { @class = "btn btn-secondary ms-1" })
        @Html.ValidationSummary(false, "", new { @class = "text-danger fw-bold mt-3" })
    </div>
    <div class="row">
        <div class="col-12">
            <div class="form-group my-3">
                <label asp-for="PCCB.Title" class="control-label fw-bold mb-1"></label>
                @Html.TextAreaFor(model => model.PCCB.Title, new { @class = "form-control", @rows = 2 })
                <span asp-validation-for="PCCB.Title" class="text-danger fw-bold"></span>
            </div>
            @*             <div class="form-group my-3">
            <label asp-for="MeetingDate" class="control-label fw-bold mb-1"></label>
            <input asp-for="MeetingDate" class="form-control" style="width:25%" onclick="this.showPicker()" />
            <span asp-validation-for="MeetingDate" class="text-danger fw-bold"></span>
            </div>
            <div class="form-group my-3">
            <label asp-for="MeetingTime" class="control-label fw-bold mb-1"></label>
            <input asp-for="MeetingTime" class="form-control" style="width:25%" onclick="this.showPicker()" />
            <span asp-validation-for="MeetingTime" class="text-danger fw-bold"></span>
            </div> *@
            <div class="form-group my-3">
                <label asp-for="PCCB.MeetingDateTime" class="control-label fw-bold mb-1"></label>
                <input asp-for="PCCB.MeetingDateTime" class="form-control" style="width:25%" onclick="this.showPicker()" />
                <span asp-validation-for="PCCB.MeetingDateTime" class="text-danger fw-bold"></span>
            </div>
            @*/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////*@
            <div class="form-group my-3">
                <label asp-for="PCCB.Invitees" class="control-label fw-bold mb-1"></label>
                @* <select id="PccbCreateInvitees" asp-for="Invitees" asp-items="@ViewBag.Employees" multiple class="form-control form-select"> </select> *@
                @* <select required id="PccbCreateInvitees" name="Invitees" multiple="multiple" asp-items="@ViewBag.Employees"></select>  *@
                 @Html.DropDownListFor(model => model.Invitees, new MultiSelectList(ViewBag.Employees, "Value", "Text"), "-- select --", new { @class = "form-control form-select", multiple=true, @id = "PccbCreateInvitees"})
                <span asp-validation-for="Invitees" class="text-danger fw-bold"></span>
            </div>
            @*/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////*@
            <div class="form-group my-3">
                <label asp-for="PCCB.Agenda" class="control-label fw-bold mb-1"></label>
                @Html.TextAreaFor(model => model.PCCB.Agenda, new { @class = "form-control", @rows = 2 })
                <span asp-validation-for="PCCB.Agenda" class="text-danger fw-bold"></span>
            </div>
@*             <div class="form-group my-3">
                <label asp-for="PCCB.Decisions" class="control-label fw-bold mb-1"></label>
                @Html.TextAreaFor(model => model.PCCB.Decisions, new { @class = "form-control", @rows = 2 })
                <span asp-validation-for="PCCB.Decisions" class="text-danger fw-bold"></span>
            </div>
            <div class="form-group my-3">
                <label asp-for="PCCB.ActionItems" class="control-label fw-bold mb-1"></label>
                @Html.TextAreaFor(model => model.PCCB.ActionItems, new { @class = "form-control", @rows = 2 })
                <span asp-validation-for="PCCB.ActionItems" class="text-danger fw-bold"></span>
            </div> *@
            <div class="form-group mt-5">
                <input type="submit" value="Create" class="btn btn-success" id="submitButtonPccbCreate" />
                @Html.ActionLink("Cancel", "Details", "ChangeRequests", new { id = Model.PCCB.ChangeRequestId, tab = "PccbReview" }, new { @class = "btn btn-secondary ms-1" })
            </div>
        </div>
    </div>
</form>

<script>
    $(document).ready(function () {
        $('#PccbCreateInvitees').select2({
            placeholder: "Select Invitee(s)"
        });
    });
</script>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

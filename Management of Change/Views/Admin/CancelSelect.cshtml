@model Management_of_Change.ViewModels.CancelChangeRequest

@{
    ViewData["Title"] = "Delete";
}

<h1>Change Request</h1>
<div class="mt-5 mb-3">
    <h4>Please Enter an MoC Number to Cancel</h4>
</div>

<div>
    <form asp-action="CancelConfirm">
        <div class="form-group my-3">
            <label asp-for="MocNumber" class="control-label fw-bold mb-1"></label>
            <input asp-for="MocNumber" class="form-control" />
            <span asp-validation-for="MocNumber" class="text-danger fw-bold"></span>
        </div>
        @*     <dl class="row">
        <dt class = "col-sm-2">
        @Html.DisplayNameFor(model => model.MocNumber)
        </dt>
        <dd class = "col-sm-10">
        @Html.DisplayFor(model => model.MocNumber)
        </dd>
        </dl>   *@

        @* <input type="hidden" asp-for="MocNumber" /> *@
        <input type="submit" value="Cancel MoC" class="btn btn-danger me-2" />
        @Html.ActionLink("Back","Index","Admin", new {}, new {@class="btn btn-secondary"})
    </form>
</div>
